# pnpm configuration for React Native / Expo compatibility
# Use isolated node_modules (pnpm default) but hoist common deps

# Hoist patterns for dependencies that need flat resolution
public-hoist-pattern[]=*@react-native*
public-hoist-pattern[]=*expo*
public-hoist-pattern[]=react-native*
public-hoist-pattern[]=@tanstack/*
public-hoist-pattern[]=jest*
public-hoist-pattern[]=*jest*
